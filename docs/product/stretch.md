1. Food Planner, Lineup, and Budget are now the highest-impact unfinished module surfaces.
2. Collaboration approval queue is still a UI stub; it needs a real backend table/RLS/policies and mutation flows.
3. Trip dashboard completion percentage is still a placeholder and should be derived from per-module progress.
4. Date entry still needs a friendlier picker/autofill flow in trip creation.
5. Delete account remains backend-only work; the frontend has confirmation UI but no destructive server flow yet.
6. Notifications and Appearance settings are scaffolded in the UI but still need real persistence and app-wide wiring.
7. Password fields should keep the visibility toggle pattern where it makes sense, especially in auth and account-upgrade flows.
8. Application cuts into the top info bar (Battery time, camera lense area)
9. Travel outfit photo upload and outfit voting are deferred for now and belong in stretch rather than the active Travel module.